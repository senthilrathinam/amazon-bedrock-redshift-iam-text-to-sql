# IAM Role-based Configuration
# No AWS credentials needed - uses EC2 instance IAM role

# AWS Region (will auto-detect from instance metadata if not set)
AWS_REGION=us-east-1

# Redshift Configuration (Auto-managed - only override if needed)
REDSHIFT_HOST=localhost
REDSHIFT_PORT=5439
REDSHIFT_DATABASE=sales_analyst
REDSHIFT_USER=admin
REDSHIFT_PASSWORD=Awsuser123$
